import React from 'react';

export default function Teams({ team }) {
  console.log(team);
  return (
    <div>
      {/* <h1>{team.name}</h1>
      <h3>{team.city}</h3>
      <h3>{team.state}</h3> */}
    </div>
  );
}
